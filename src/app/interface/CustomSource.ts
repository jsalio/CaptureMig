/**
 * Represents a custom data source configuration
 *
 * @export
 * @interface CustomSource
 */
export interface CustomSource {
  procedureName: string;
  usedParameters: boolean;
  customReportSourceTitle: string;
  active: boolean;
}
