/**
 * Represent result for delete page/document/batch operation
 * @export
 * @interface DeleteResult
 */
export interface DeleteResult {
    isDeleted: boolean;
    message: string;
    messageHeader: string;
  }
