/**
 * Represents a basic operation response
 *
 * @export
 * @interface CommentDocumentModal
 */
export interface CommentDocumentModal {
    message: string;
    title: string;
    question: string;
    buttonMessage: string;
    buttonMode: string;
}
