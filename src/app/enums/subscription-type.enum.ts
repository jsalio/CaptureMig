/**
 * Represents a subscribe element notification type
 * @export
 * @enum {number}
 */
export enum SubscriptionType {
    /**
     * Represents the subscription to the document element
     */
    Document = 'Document',

    /**
     * Represents the subscription to the batch element
     */
    Batch = 'Batch'
}
