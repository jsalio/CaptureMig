/**
 * Represents the type of delete to apply in document viewwer
 * @export
 * @enum {number}
 */
export enum DeleteMode {
    DeletePage = 'DeletePage',
    DeleteDocument = 'DeleteDocument',
    DeleteBatch = 'DeleteBatch'
}
